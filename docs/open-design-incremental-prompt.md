## 新增需求

### 1. 色域工具

在色轮工具页面新增一个 Tab，放一个色域工具。

- 一个实心圆形色盘：外圈是饱和纯色（色相环），越往中心越灰（饱和度递减）
- 用户在色盘上放置 3 个或 4 个锚点，连线形成三角形或四边形区域
- 锚点可拖拽，区域实时更新
- 取色器可在区域内或边缘取色，取到的 RGB 送入混色引擎（和色轮取色的后续流程一样）

### 2. 预览状态保持

3D 预览页切换到其他页面再返回时，恢复上次的几何体、材质、光源等状态，而不是重置为默认。
