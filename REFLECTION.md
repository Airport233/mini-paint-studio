# REFLECTION.md — HobbyMix 项目反思报告

> 报告使用AI 辅助生成框架草稿以及对我攥写的内容格式、错字进行修正。

---

## 一、Superpowers 技能：哪些有效，哪些形式大于实质

### 真正发挥作用的技能

- **brainstorming**：确实有一定的作用，帮我理清了我想要开发的产品的思路，但是仍然有局限性，比如他往往会专注一产品关键需求，但忽略登录注册、数据持久化等业务的骨架功能，如果没有及时的打断agent，agent会在bs阶段结束后立刻开始执行后续步骤，导致产品变成一个demo。
- **writing-plans**：降低了代码的出错性，基本上不是很复杂的功能plan了之后只需要略微调试。
- **test-driven-development**：可能有作用，但是体感不明显，比如代码出错后agent的修复比较无感知。

### 感觉形式大于实质的技能

- **subagent-driven-development**：确实有用，在项目中派发 subagent 答复降低了上下文的长度，避免了注意力的分散以及固有的认识偏见，但是可能是因为项目本身不大，所以不派发subagent，agent本身也基本能完成大多数模块的开发。
- **requesting-code-review**：有一定作用，但不多，主要还是依靠人工从前端手动测试。agent自测智能保证代码符合plan且没有明显漏洞，但是对业务功能没有人类灵活的理解。
- **verification-before-completion**：体感不明显：大多数的验证的纪律是自己建立的（语法检查、后端测试），跟技能触发关系不大。

### 为什么有些技能没起作用
技能触发有时候比较依赖ai的自觉，没有来自用户的明确的指令，比如必须执行某某某skill，即使是模糊的指令比如按照作业要求XXX的步骤执行，AI还是会不自觉地略掉一些他认为不重要的步骤。而且在以结果为导向的编程过程中，很难被及时发现。

---

## 二、TDD 在 AI 协作下的体感：阻碍还是放大器

### 后端 TDD 体验

后端的 TDD 流程其实是整个项目里执行得最规范的部分。Phase 1-3 每个 task 都是先写测试、再跑红、再写实现、再跑绿。MixService 的混色算法测试是个好例子——6 个 test case 覆盖了精确匹配、灰度兜底、少量标记、CMY 参考、性能上限，compile error → implementation → 6 PASS 整个流程很顺畅。AI 对"先写测试再写实现"这套似乎很擅长。可能是因为测试代码本身也是代码。

但存在的问题是：正确的代码和让测试通过的代码之间，还是有很大的差距的。有时候测试通过了，但仔细一看，实现是硬编码的或者逻辑有漏洞，只是恰好覆盖了测试 case。你可能不仔细 review 就发现不了。AI的case可能也比较局限，但这部分我没有看过他的case怎么写的，所以我也不是很确定。

总的来说算是放大器，至少避免了一部分错误的出现，且耗时和耗token并不多。但验证不了复杂的业务逻辑。

---

## 三、Subagent-driven 工作流的实际表现

### 什么时候派发了，什么时候没派发

回头来看，subagent 在这个项目里的使用是有选择性的。Phase 3、4、6的后端核心模块开了 worktree 独立开发，确实起到了隔离作用——主分支不受feature开发的commit干扰，merge回来也很干净。Phase 7的前端部分也派发了 subagent来写 presets.html和 preview.html的集成代码，产出质量不错，review后只发现了几个具体问题。

但大量的小修补——比如 Phase 4 的STL模型的YZ轴映射反复修、Phase 6的标签下拉框遮挡、配方卡片样式调整等都是直接在主对话里改的。这些小task如果每个都开一个 subagent，反而会增加等待和 review 的摩擦。项目总共也就 3000-5000 行有效代码，感觉拆太细没意义。

### 偏题的具体案例

偏题大部分不是 subagent 的问题，是 prompt 的问题。没有什么深刻印象了。

### 什么粒度最合适

我的体感是：一个 task 应该是一个"独立可验证的功能单元"，而不是一个文件或一个函数。比如"创建 LightingPreset 后端 CRUD"是一个合适的 task——有明确的输入输出、能跑测试验证、不依赖前端。但"给配方列表页加标签筛选"拆成"改 CSS"+"改 HTML"+"改 JS"三个 task 就太碎了，AI 反而容易在跨文件的上下文切换中出错。

太小的修改不值得派发subagent，太复杂我没有尝试过，想来效果不会很好，毕竟subagent隔离了上下文后在一定程度上我认为也缺失了主对话中隐藏信息。

---

## 四、SPEC 与 PLAN 质量对实现的影响

spec和plan最大的作用是用规范化的语言记录了bs阶段对需求和系统质量属性的一些描述，其中对后者的实现较好，对前者一般。假设一个需求可以拆分为多个动作，那么每个动作spec和plan都能规定好，AI也能按照约束实现，但是当这些动作一多，一衔接起来，就会出现意想不到的低耦合问题。导致只能在前端实际测试业务流程的时候才能发现，然后需要逐个bug反复让AI去修改。但是让subagent跑偏到不太会，主要我一共做了两次作业，两次中我在bs阶段和AI讨论项目需求的时候，都绞劲脑汁的完善方方面面，所以体现在后续阶段由于spec和plan不规范导致的问题基本没有遇到过。

---

## 五、最有效的 Prompt / Context 策略

说实话，我没有什么"精心设计的 prompt 模板"。大部分时候就是直接描述需求，让 AI 去执行。但有两个习惯在项目中反复被证明有效。

### 策略一：让 AI 解释自己的操作理由

我要求agent每次修改后必须说清楚"为什么这样改"和"改了什么"。不是要它写长篇文档，就是一句话解释。这迫使 AI 在改代码前多了一步推理，而不是机械地替换字符串，有效的让来回次数明显减少了。

### 策略二：跨智能体验证

Phase 7 的 STL 标签闪烁 + localStorage 覆盖问题，Claude Code 连续 7 轮修复都没彻底解决——每次修好一个环节，另一个环节又漏了。最后我让Claude Code 将逐个问题整理为md格式的一段 prompt 描述丢给 GPT-5.2-Codex，它在全新 session 里一次就定位到了根因。Claude 一直在"调用方"修——加标志位、改回调顺序、加防抖。Codex 直接从"被调用方"修——让函数自己记住状态。两种思路的差异不是能力问题，是视角问题，是不同模型对代码的能力不同导致的。让 AI 总结 prompt 换个 agent 尝试还是非常有效的。

### 其他

除了上面两点，我觉得在我的使用习惯中 prompt 策略没有太多定式，更多时候是根据当前需求临场发挥，不是什么都能提前设计好。

---

## 六、Open Design 的使用评价

### 设计系统的价值

对前端设计帮助非常大，主要是他可以预览前端原型，同时基于选择元素、标注评论、画示意图等方式，让人与AI在前端设计的一些文字难以度量表示的问题有了新的沟通方案，让前端的设计的便利化了。

### 消除"AI 千篇一律"了吗

相对来说我认为至少消除了一些AI设计前端的常见问题，如美观度低、喜欢用圆角卡片+色块填充的廉价习惯。但是它本身提供的模板实际上也是很套路化的，只能说胜在简洁美观且没有廉价感。

---

## 七、如果重做，会改变哪些做法

- Phase 0 就从 subagent 派发开始，而不是后期才补
- 每个 Phase 开 worktree 的同时创建 PR（Phase 2 漏了 PR）
- 两阶段评审从第一个 task 就强制执行
- 不再是让 agent 自己去读流程要求然后「严格按照要求」，而是直接在对话框里明确指定步骤顺序，避免 agent 自作主张遗漏步骤。
- 让AI明确的在本地启动一个数据库，避免每次测试都要重新注册。

---

## 八、对 Superpowers 方法论的批判

### 它假设了什么

1. **AI 会主动遵循技能路由** — 假设 PLAN.md 里写了 `subagent-driven-development`，AI 就会在执行时自动触发。实际上 AI 倾向于最小阻力路径。
2. **用户能感知流程偏离** — 假设用户会注意到 subagent 没派发、评审没触发。实际上代码在正常产出，偏离很难被察觉。
3. **task 颗粒度足够小** — 假设 2-5 分钟的 task 足够细。但实际中很多 task 涉及跨文件修改，远超预估。
4. 用户有足够的token，经得起在每个模块大量燃烧（实则对学生不然）。

### 这些假设在你的项目里成立吗

- 假设 1：**不成立**。168 commits 都标了 Subagent，但从未真正派发。（参考 AGENT_LOG §3）
- 假设 2：**不成立**。直到 Phase 6 收尾审计才发现。
- 假设 3：**部分成立**。后端 TDD task 颗粒度合适，前端集成 task 经常超时。
- 假设 4：**部分成立**：尽管严格按照框架走确实很耗经费。

### Superpowers 做得好的地方

- brainstorming 的追问机制确实能逼出遗漏需求
- TDD 强制让后端代码质量稳定
- worktree + PR 工作流对隔离开发确实有效

### Superpowers 的盲区

- brainstorming 的追问会忽视crud等基础模块，进而影响到后续的agent自主编程。
- 技能触发缺乏强制机制——依赖 AI"自觉"
- 对前端项目和实际业务流程的测试支持不足
- commit 标注容易变成形式主义

---

## 九、对当前 AI4SE 工具与方法论的整体看法

### 一个人 + 智能体的产能

在这个项目里，AI 对效率的提升是明显的，但提升集中在"写代码"这个环节，不是"做产品"。大量时间实际上是花在了产品方向纠偏（比如 Open Design 生成 React 工程被我否决）、实际业务流程验证（每个 Phase 结束我都要从前端点点点看能不能跑通）、以及跟 AI 的异步 bug 拉锯上。AI 帮写了一万行代码，但你要花更多的时间去确认这一万行代码是对的，认知负荷大幅提高，尤其是有时AI对代码的改动速度远超过人的审核速度，如果没有充分的项目编码经验很容易跟不上AI的节奏。

### AI4SE的优劣

AI 写 CRUD、写测试、写标准算法（混色引擎的 RGB 枚举）非常快且质量稳定。但一旦涉及复杂的状态管理，异步时序问题，AI 就会在一个圈子里反复打转。在Phase 7 的 STL 标签 bug 是典型——AI 连续修复了 7 轮，每一轮都解决了一个小环节，但整体问题一直没根治。说明AI对复杂项目结构和非模板化的特异的功能处理上能力还不充分，且AI有时喜欢走一些“快乐路径”的编码，隐患较大。

### 人类的不可替代性
所以人类在AI4SE的浪潮下，至少还有几个作用暂时无法取缔：
产品需求评估、代码质量把关（背锅）以及对agent行为的监督和测试。需要对agent工作的过程加以严格约束，避免agent以目的为导向产生的不可见隐患。

### 对未来工具链的期待

比较想要两个东西：一是强制流程执行——比如在 plan 阶段就让用户勾选"这个 Phase 必须派发 subagent"，然后工具在每次 task 前自动提示，不做就不能 continue。二是不那么烧钱的 agent——校供环境下基本够用，但对于自担经费的学生来说，严格走完 Superpowers 的全流程 token 消耗还是偏高。
